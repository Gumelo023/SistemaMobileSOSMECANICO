<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Cor de fundo do campo de feedback -->
    <solid android:color="#494C4E" /> <!-- Ajuste a cor conforme necessário -->

    <!-- Definir bordas arredondadas -->
    <corners android:radius="8dp" />

    <!-- Margem opcional interna (padding) -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" />
</shape>
